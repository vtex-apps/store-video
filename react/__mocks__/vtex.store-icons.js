import React from 'react'

export const IconPlay = (id) => {
  return <div id={id} />
}

export const IconPause = (id) => {
  return <div id={id} />
}

export const IconExpand = (id) => {
  return <div id={id} />
}

export const IconVolumeOn = (id) => {
  return <div id={id} />
}

export const IconVolumeOff = (id) => {
  return <div id={id} />
}
