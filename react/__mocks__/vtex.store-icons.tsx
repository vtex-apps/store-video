import React from 'react'

export const IconPlay = (id: string) => {
  return <div id={id} />
}

export const IconPause = (id: string) => {
  return <div id={id} />
}

export const IconExpand = (id: string) => {
  return <div id={id} />
}

export const IconVolumeOn = (id: string) => {
  return <div id={id} />
}

export const IconVolumeOff = (id: string) => {
  return <div id={id} />
}
